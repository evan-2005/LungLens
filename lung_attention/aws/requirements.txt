# Pinned to the laptop that produced the paper's numbers (Python 3.11.9).
# torch / torchvision are installed separately from the CUDA 12.4 wheel index
# by setup_instance.sh, matching torch 2.6.0+cu124 on the laptop.
numpy==2.4.4
pillow==12.2.0
opencv-python-headless==4.13.0.92
scikit-learn==1.8.0
scipy==1.17.1
tensorboard==2.20.0
kagglehub==1.0.1
gradio==6.14.0
matplotlib==3.10.9
